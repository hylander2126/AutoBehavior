"""
policies package – LLM-generated controller code.

At runtime the training loop writes generated policy modules here and imports
them dynamically.  The ``BasePolicy`` class defines the interface every
generated policy must satisfy.
"""
